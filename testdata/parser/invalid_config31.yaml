logger:
  level: 2
  dest: "test"

netbox:
  apiToken: "netbox-token"
  port: 666
  hostname: netbox.example.com
  httpScheme: "http"
  arpDataLifeSpan: -10 # Error arpDataLifeSpan Must be positive

source:
  - name: paloalto
    type: paloalto
    hostname: paloalto.example.com
    username: user
    password: pass
    collectArpData: true

  - name: fortigate
    type: fortigate
    hostname: forti.example.com
    apiToken: "dsafdsfdsafasdffdsadf"
