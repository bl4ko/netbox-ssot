logger:
  level: 2
  dest: "test"

netbox:
  apiToken: "netbox-token"
  port: 666
  hostname: netbox.example.com
  httpScheme: "http"
  caFile: "wrong path" # error
  removeOrphans: True
  removeOrphansAfterDays: 5 # error because removeOrphans is set to True
